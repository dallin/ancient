<!DOCTYPE html>
<html lang="{{lang}}">
    <head>
        {{ghost_head}}
        <title>{{@blog.title}}</title>
        <link rel="stylesheet" type="text/css" href="{{asset '/styles/ancient.css'}}">
    </head>
    <body class="{{body_class}}">
        <header>
            <h1>{{@blog.title}}</h1>
            <aside>{{@blog.description}}</aside>
            {{navigation}}
        </header>
        <div class="wrapper">
            {{!-- All the main content gets inserted here, index.hbs, post.hbs, etcetera. --}}
            <main class="main">
                {{{body}}}
            </main>
        </div>
        <hr>
        Copyright &copy; {{date format="YYYY"}} Dallin Hitchcock. All rights reserved. 
        {{ghost_foot}}
    </body>
</html>